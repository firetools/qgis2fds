[pytest]
# Run the complete suite by default; either directory can still be selected.
testpaths = tests/unit tests/integration
pythonpath = .. tests/unit tests/integration
addopts = --strict-markers -ra
# Custom pytest setting, read by tests/conftest.py 
# Use native QGIS:
# qgis_process = /usr/bin/qgis_process
# Use flatpak QGIS:
qgis_process = flatpak run --command=qgis_process org.qgis.qgis
# FDS executable used to validate every exported integration case:
fds = /var/home/egissi/.local/opt/FDS/FDS6/bin/fds
# Set to true to run each exported case through FDS (slow):
run_fds = false
markers =
    integration: runs installed QGIS and qgis2fds, with optional FDS validation
